package sec02.verify;

public class Type {
	/*
	 * Q1
	 ### 타입 변환
	 
	 
	 ## 기본 타입
	 
	 # 자동 타입 변환
	 	int a = 10;
	 	bouble b = a;
	 # 강제 타입 변환
	 	a = (int) b;
	 
	 
	 ## 참조 타입
	 
	 # 자동 타입 변환
	 
	 # 강제 타입 변환
	
	 ## 인터페이스 타입 변환
	 
	 	인터페이스 > 구현클래스
	 	
	 	interface A{ }
	 	class B implements A { }
	 	= A > B
	 	
	 	1-1)
	 	A a = new B();
	 	(interface type) name = new (구현 객체) 는 가능하다.
	 */
	
}
